{
  "name": "@heroor/tpm",
  "version": "0.0.2",
  "description": "TPM is a typings package manager for your npm, pnpm and yarn projects",
  "author": "Bener",
  "license": "MIT",
  "main": "./dist/index.js",
  "bin": {
    "tpm": "./bin/tpm"
  },
  "scripts": {
    "dev": "tsc -p tsconfig.json -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "execa": "^6.1.0",
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "@types/node": "^17.0.26"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "files": [
    "dist",
    "bin"
  ]
}
